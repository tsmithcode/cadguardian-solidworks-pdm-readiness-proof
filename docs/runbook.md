# Runbook

1. Run `npm run verify`.
2. Run `npm run demo`.
3. Review `reports/demo-validation-report.json`.
4. Replace synthetic fixtures only inside a private, approved AgentOps project when real files are authorized.
