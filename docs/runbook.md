# Runbook

1. Run `npm run doctor`.
2. Run `npm run verify`.
3. Run `npm run demo`.
4. Review `reports/demo-validation-report.json`.
5. Replace synthetic fixtures only inside a private, approved AgentOps project when real files are authorized.
